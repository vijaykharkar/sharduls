# App Settings
APP_NAME=SHARDUL-GE E-commerce API
APP_VERSION=1.0.0
DEBUG=True
API_V1_PREFIX=/api/v1

# Database Settings
DATABASE_URL=postgresql://postgres:sumasoft123@localhost:5432/ecommerce_db
DATABASE_POOL_SIZE=10
DATABASE_MAX_OVERFLOW=20

# Redis Cache Settings
REDIS_URL=redis://localhost:6379/0
CACHE_EXPIRE_TIME=3600

# JWT Settings
SECRET_KEY=your-secret-key-change-this-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# CORS Settings (comma-separated list)
CORS_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:5174

# Email Settings
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM_EMAIL=noreply@shardul-ge.com
SMTP_FROM_NAME=SHARDUL-GE

# File Upload Settings
UPLOAD_DIR=uploads
MAX_UPLOAD_SIZE=5242880
ALLOWED_EXTENSIONS=jpg,jpeg,png,gif,pdf

# Payment Gateway Settings
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=
STRIPE_SECRET_KEY=
STRIPE_PUBLISHABLE_KEY=

# Celery Settings
CELERY_BROKER_URL=redis://localhost:6379/1
CELERY_RESULT_BACKEND=redis://localhost:6379/2

# Pagination Settings
DEFAULT_PAGE_SIZE=20
MAX_PAGE_SIZE=100

# Security Settings
PASSWORD_MIN_LENGTH=8
PASSWORD_REQUIRE_UPPERCASE=True
PASSWORD_REQUIRE_LOWERCASE=True
PASSWORD_REQUIRE_DIGIT=True
PASSWORD_REQUIRE_SPECIAL=True

# OTP Settings
OTP_EXPIRE_MINUTES=10
OTP_LENGTH=6

# Product Settings
MIN_PRODUCT_PRICE=0.01
MAX_PRODUCT_QUANTITY=10000
